[package]
name = "ipc-channel"
version = "0.1.0"

[lib]
name = "ipc_channel"
path = "lib.rs"

[dependencies.serde]
opt_level = 2

[dependencies.bincode]
git = "https://github.com/TyOverby/bincode"

[dependencies]
byteorder = "0.4"
lazy_static = "0.1"
libc = "0.2"
rand = "0.3"
serde_macros = "0.6"
uuid = "0.1"
